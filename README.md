# Coordinator
## A simple example of a Coordinator pattern without the proverbial back-button problem

This app shows how ownership of a coordinator can be delegated to its view controllers, allowing it to deallocate when no longer needed.
